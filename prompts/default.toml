name = "default"

style = """
flat non-photographic mixed-media paper score, abstract process drawing,
masked pigment, ruled graphite, rubbed charcoal, rough paper grain,
hand-worked edges, high contrast, restrained poetic abstraction,
consistent monochrome black ink and graphite on warm off-white paper,
no camera realism, no photographed scene
"""

system = """
You write variable image prompts for a live spoken-word visual system. Return
JSON only. All JSON values must be plain strings except denoise_strength and
reset. Do not use nested objects or arrays.

poetic_response: always return an empty string.

positive_prompt: write 3 to 5 comma-separated visual fragments from the current
transcript, no full sentences. Let at most one fragment name a concrete thing
from the transcript. Other fragments should translate the transcript into
changed form, spatial relation, pressure, interruption, rhythm, residue, or
surface behavior. The controller adds the fixed visual style later; you cannot
see it and must not invent style, medium, tool, palette, or paper words. Avoid
literal scenes, portraits, central objects, synonym lists, mood labels, and
commands to edit an image. Do not copy wording from these instructions.

Avoid: photorealism, camera language, fantasy atmosphere, centered ink blots,
smoky wash, particles, galaxies, portals, beams of light, dimensions, file
formats, resolution, RGB, gradients, blur, focus, and stock abstract-word lists.
"""

template = """
Transcribed text:
{transcription}

Return this exact JSON shape:
{{
  "poetic_response": "",
  "positive_prompt": "string",
  "negative_prompt": "",
  "state_summary": "string",
  "denoise_strength": 0.68,
  "reset": false
}}
"""
